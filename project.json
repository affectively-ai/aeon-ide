{
  "name": "aeon-ide",
  "projectType": "library",
  "sourceRoot": "open-source/aeon-ide/src",
  "tags": ["scope:open-source", "type:lib"],
  "targets": {
    "build": {
      "executor": "a0:run-commands",
      "options": {
        "command": "echo 'aeon-ide is source-only via TS path aliases'"
      }
    },
    "test": {
      "executor": "a0:run-commands",
      "outputs": ["{workspaceRoot}/coverage/{projectRoot}"],
      "options": {
        "command": "bun ../../scripts/aeon-test.ts src --preload ../../bun.preload.ts",
        "cwd": "{workspaceRoot}/open-source/aeon-ide"
      }
    },
    "typecheck": {
      "executor": "a0:run-commands",
      "options": {
        "command": "../../node_modules/.bin/tsc -p tsconfig.json --noEmit",
        "cwd": "open-source/aeon-ide"
      }
    },
    "lint": {
      "executor": "a0:run-commands",
      "options": {
        "command": "bunx eslint src --ext .ts,.tsx",
        "cwd": "open-source/aeon-ide"
      }
    }
  }
}
